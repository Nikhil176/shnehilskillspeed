<%= form_for(sales_team) do |f| %>
  <% if sales_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sales_team.errors.count, "error") %> prohibited this sales_team from being saved:</h2>

      <ul>
      <% sales_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %>
    <%= f.text_field :Name %>
  </div>

  <div class="field">
    <%= f.label :Course %>
    <%= f.text_field :Course %>
  </div>

  <div class="field">
    <%= f.label :Customer %>
    <%= f.text_field :Customer %>
  </div>

  <div class="field">
    <%= f.label :Status %>
    <%= f.text_field :Status %>
  </div>

  <div class="actions">
    <%= f.submit 'Submit', class: 'btn btn-default' %>
  </div>
<% end %>
